# HomeGarden Backend Installation Guide

## Prerequisites
- Node.js v18+
- npm v9+
- MongoDB Atlas account (already configured)
- Cloudinary account (already configured)

## Step 1: Install Dependencies

```bash
cd backend
npm install